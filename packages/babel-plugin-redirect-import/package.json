{
  "name": "babel-plugin-redirect-import",
  "description": "A babel plugin to redirect import modules or specifiers",
  "version": "0.2.0",
  "main": "lib/index",
  "types": "lib/index",
  "files": [
    "lib"
  ],
  "scripts": {
    "build": "pnpm run compile",
    "clean": "rimraf ./lib *.tsbuildinfo",
    "compile": "tsc -b tsconfig.build.json",
    "prepublishOnly": "pnpm run build"
  },
  "dependencies": {
    "babel-shared": "0.2.0"
  },
  "devDependencies": {
    "babel-plugin-tester": "10.0.0",
    "rimraf": "4.1.0",
    "typescript": "4.9.4"
  }
}
